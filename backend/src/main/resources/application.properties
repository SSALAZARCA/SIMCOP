
spring.application.name=simcop-backend

# Database Configuration (Remote MySQL - TEMPORARILY DISABLED DUE TO TIMEOUT)
# spring.datasource.url=${DB_URL:jdbc:mysql://srv1196.hstgr.io:3306/u689528678_SIMCOP?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
# spring.datasource.username=${DB_USERNAME:u689528678_SIMCOP}
# spring.datasource.password=${DB_PASSWORD:Ssc841209*}

# Local H2 In-Memory Database (Fallback)
spring.datasource.url=jdbc:h2:mem:simcopdb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true
# Flyway disabled for H2
spring.flyway.enabled=false

# Server
server.port=8080

# JWT Configuration
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=86400000

# OpenWeatherMap Integration
openweather.api.key=${OPENWEATHER_API_KEY:874fcaa4523b2315647588bd613e228e}
openweather.api.url=https://api.openweathermap.org/data/2.5/weather
telegram.bot.token=

# File Upload Configuration
file.upload-dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB